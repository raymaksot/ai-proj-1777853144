A straightforward LRU (Least Recently Used) cache implemented in Python using `collections.OrderedDict`. It supports `get` and `put` operations with O(1) average time complexity and automatically evicts the least recently used item when the capacity is exceeded. The cache tracks total hits and misses, and provides a `stats()` method to inspect the access pattern. No external dependencies are required.